{
  "name": "templater",
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "templater": "dist/bin.js"
  },
  "dependencies": {
    "@templater/core": "workspace:*",
    "commander": "^14.0.1"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts",
    "test": "echo \"no tests yet\""
  }
}
